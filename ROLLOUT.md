# GIGA Standard v4 ロールアウト記録

| リポジトリ | 型 | P0 | P1(表示/PWA) | P2 | P3 | ゲート | 備考 |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| Werewolf | B | ✅ | ✅ | ✅ | ✅ | ✅ | 2026-08-03 実施。監査 → P0〜P4 を通し |

## Werewolf でやったこと（他リポジトリへ展開するときの参考）

### 所要と粒度

監査（AUDIT.md）→ P0 → P1 → P2 → P3 → P4 を、**フェーズごとに1コミット**で分けた。
本来は1フェーズ＝1PR が望ましいが、今回は作業ブランチが `claude/rollout-ylgn3k` の
1本に指定されていたため、コミット単位で分離している。
どのコミットも単独で revert できる形になっている。

### このリポジトリで実際に効いた修正（他でも同じ問題が出そうな順）

1. **manifest の `id` 未設定**
   `vite-plugin-pwa` の manifest は `id` を書かないと出力されない。`start_url`/`scope` も
   `'.'` のままになりがちで、同一オリジンを共有する `gigayama.github.io` では事故域。
   → `vite.config.ts` に `REPO_BASE` 定数を置き、3点をそこから組み立てる形にした。
   **Vite 製の他リポジトリでも、まずここを確認するとよい。**

2. **Google Fonts の `@import`**
   `src/index.css` の1行目という、いちばん見落としやすい場所にあった。
   CSS 内の `@import` は描画開始を遅らせるうえ、CSP を張れなくする。
   → `scripts/build-fonts.mjs` でサブセットを取得して自己ホスト化。
   このスクリプトは family 名と収録文字を変えるだけで他リポジトリへ移植できる。

3. **`registerType: 'autoUpdate'`**
   `vite-plugin-pwa` の例によく出てくる指定で、そのまま残っていた。
   授業中に黙って表示が変わるため、`'prompt'` + 更新通知に変えるべき。

4. **アイコンの生成元と配信物が同じファイル**
   `public/favicon.png` が両方を兼ねていると、圧縮した時点で元画像が失われる。
   → 元画像を `assets/` に移し、`public/` は毎回生成する形へ。

5. **`w-12 h-12` のタップ領域**
   ルート `font-size` を `clamp()` で可変にしているアプリだと、`rem` 指定のボタンは
   狭い端末で 44px を割る（3rem × 14px = 42px）。**px の絶対値で下限を置く**必要がある。

6. **狭い画面での折り返し**
   ふりがな付きのボタン（「10人（にん）」）は、ルビのぶん横に広い。
   320px 幅では 3列グリッドのセルが約 63px しかなく、既定の余白だと必ず折れる。

### 展開する前に決めておくこと

- フォントのサブセットに何を入れるか（本リポジトリでは「アプリが出す文字＋かな全部＋
  英数記号（半角/全角）＋小学1〜2年の漢字」で 797字・2ウェイト計 196KB）
- `quality.config.json` の `budgets` をリポジトリごとに変えるか、共通にするか
- `scripts/check-project.mjs` の正本をどこに置くか（現状は各リポジトリにコピー）

### 検証の手順（P1 のあと必ず実施する）

```bash
npm run build
npx serve dist -p 8000      # ※ manifest の scope に合わせたパスで配信すること
```

- [ ] 320px 幅で横スクロールが出ない（DevTools のレスポンシブモード）
- [ ] `application` タブで manifest が読め、インストール可能と表示される
- [ ] オフラインにして再読み込みしても起動する
- [ ] コンソールに `Refused to`（CSP 違反）が 0 件
- [ ] コンソールにエラー 0 件

本リポジトリでは、この5項目を Chromium（Playwright）で自動化して確認した。
検証スクリプトは依存を増やさないためリポジトリには入れていない。
